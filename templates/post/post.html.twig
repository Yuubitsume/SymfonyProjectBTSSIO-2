 {% block stylesheets %}
    <link href="{{ asset('html/post.css') }}" rel="stylesheet" /> 
{% endblock %}
    <div class="table-responsive">
        <table class="table">
            <thead>
            </thead>
            <tbody> 
    
                        <th id="t">Surname</th>
                        <th id="t">Name</th>
                        <th id="t">Subject</th>
                        <th id="t">Grade</th>
                        <th id="t">coefficient</th>
                {% for note_controle in note_controles %}
                    <tr>
                        <td id="th1">{{ note_controle.NomEleve }}</td>
                        <td id="th2"> <a href="{{ path('app_eleve') }}">{{ note_controle.PrenomEleve }}</td>
                        <td id="th3">{{ note_controle.NomMatiere }}</td>
                        <td id="th4">{{ note_controle.Note}}</td>
                        <td id="th5">{{ note_controle.Coefficient}}</td>
                    </tr>
                {% endfor %}
            </tbody>   
        </table>
    </div>
<div class="col-lg-12 text-center mt-5">
    <a href="{{ path('app_note') }}" class="text-danger add">Ajouter une note</a>
</div>